# GEO 快速计算 A/B 实验（2026-07-25）

## 基线与测试口径

- 基线提交：`4169ec1`（2026-07-13 最好版恢复代码）
- 算例：`geo_B10_N300_R30-26-1.txt`
- seeds：1–10
- 编译器：Apple Clang 21.0.0
- 基础参数：`-O3`
- 轨迹判据：逐 seed 比较全部 `globalProfit@phase`
- 正确性判据：`Solution verified OK`，并核对全部非计时 PROFILE 计数

筛选实验给基线和每个候选都设置相同的 120-phase 编译期测试上限。该测试
钩子只用于实验，最终生产源码中已经移除。

## 单项实验

| 实验 | 十 seed 总 CPU 时间 | 相对基线 | 轨迹 | 结论 |
|---|---:|---:|---|---|
| 基线 | 120.106 s | — | 基准 | — |
| 显式缓存 `oldOverBW/PW` | 122.365 s | 慢 1.88% | 10/10 一致 | 不保留 |
| 整数化 `relaxed_rem_ok` 阈值 | 110.868 s | 快 7.69% | 10/10 一致 | 保留 |
| 整数阈值 + 提前禁忌判断 | 115.113 s | 比整数阈值慢 3.83% | 10/10 一致 | 不保留 |
| 整数阈值 + 局部统计计数 | 107.421 s | 快 10.56% | 10/10 一致 | 保留 |
| 上述最终源码 + LTO | 110.722 s | 比最终源码慢 3.07% | 10/10 一致 | 不采用 |
| 上述最终源码 + PGO | 113.270 s | 比最终源码慢 5.44% | 10/10 一致 | 不采用 |

整数阈值先用乘法得到候选边界，再用原始浮点除法在边界两侧校正。因此
运行时的整数比较与原始 `(double)overload / capacity <= rho` 判定一致。

局部统计计数只改变计数写入位置：混合搜索结束时一次性写回
`g_tabuBlock`、`g_crossCand`、`g_crossRelocCand` 和
`g_crossSwapCand`。最终计数与基线逐 seed 完全一致。

## 受控最终 A/B

单进程按 `baseline seedN → optimized seedN` 顺序成对运行，每次固定
120 phases：

| seed | 基线 CPU 秒 | 优化 CPU 秒 | 节省 | 吞吐提升 |
|---:|---:|---:|---:|---:|
| 1 | 9.58664 | 8.61840 | 10.10% | 1.112x |
| 2 | 10.58090 | 9.41158 | 11.05% | 1.124x |
| 3 | 9.65271 | 8.68017 | 10.08% | 1.112x |
| 4 | 11.08360 | 9.91093 | 10.58% | 1.118x |
| 5 | 10.75170 | 9.53478 | 11.32% | 1.128x |
| 6 | 9.81804 | 8.79191 | 10.45% | 1.117x |
| 7 | 9.92826 | 8.98368 | 9.51% | 1.105x |
| 8 | 9.41842 | 8.45621 | 10.22% | 1.114x |
| 9 | 10.54630 | 9.37906 | 11.07% | 1.124x |
| 10 | 10.07590 | 9.00453 | 10.63% | 1.119x |

合计从 101.44247 秒降到 90.77125 秒，CPU 时间减少 **10.52%**，
等价于吞吐提升 **1.118x**。十个 seed 的完整收益/phase 轨迹、最终解和
全部非计时 PROFILE 计数一致。

另做两轮同时负载的 30 秒 A/B，并在第二轮交换启动顺序：

- 基线共完成 4133 phases；
- 优化版共完成 4543 phases；
- 相同时间内 phase 数提高 **9.92%**；
- 两轮二十组共同轨迹全部一致。

## 600 秒验证

最终候选完成 seeds 1–10 的 600 秒运行，10/10 均通过解校验；最终收益为：

`27715, 27695, 27701, 27690, 27704, 27698, 27639, 27685, 27666, 27663`

平均 27685.6、样本标准差 23.02、最好 27715。所有共同 phase 上的轨迹
与基线一致。

该轮 600 秒运行是在连续满核实验后执行，与前一天基线不属于同一受控
时段，因此其 phase 数不能用于微优化测速；速度结论以单进程固定 phase
配对 A/B 和同负载双向启动 A/B 为准。

## `005843` 与 `221106` 的 330 份日志复核

逐一对齐
`batch_results_20260725_005843/logs` 和
`batch_results_20260713_221106/logs` 后得到：

- 330/330 的 `after_greedy` 和 `after_LS1` 完全一致；
- 326/330 在双方共同完成的 phase 范围内，全部 `profit@phase` 一致；
- 其余 4 份只在 600 秒截止所在的最后一个未完整完成 phase 内不同，
  前面的收益/phase 序列仍一致；
- 最终收益按 seed 统计为 8 份变好、312 份持平、10 份变差；
- 新/旧完成 phase 数比值的中位数为 1.012，最小 0.652，最大 1.143。

变差不是快速计算改变了搜索逻辑。最直接的反证来自未改动的严格搜索：
新批次 330/330 的 `us_per_ls_iter` 都比历史批次高，新/旧比值中位数
为 1.084，最差为 1.824。与此同时，已经优化的混合搜索单位迭代耗时
比值中位数为 0.945，说明代码优化本身有效，但被运行环境的降速部分抵消。

Shell 历史记录表明整批使用了 `JOBS=110`。330 个 600 秒任务约 31 分钟
完成，也对应约 106 个同时占用 CPU 的进程。110 路进程共享频率、SMT
执行资源和缓存时，不同进程的减速并不均匀。程序用 `clock()` 限时；
调度等待不计入 CPU 时间，但降频、SMT 争用和缓存失效率上升会减少每个
CPU 秒能完成的指令数。因此跨日期、110 路并发的固定 600 秒结果不能用来
判断几个百分点的源码优化。

## 第二轮等价优化

继续以提交 `d238f9a` 为基线，使用同一算例、seed 1--10、固定 120 phase
的单进程交叉启动 A/B，筛选结果如下：

| 候选 | 结果 | 完整轨迹/计数 | 处理 |
|---|---:|---|---|
| 严格搜索计数器手工局部化 | 慢 0.32% | 10/10 一致 | 不保留 |
| 精确超载查表 | 慢 1.80%（先测 3 seeds） | 3/3 一致 | 不保留 |
| 预计算 double 容量分母 | 快 2.07% | 10/10 一致 | 保留 |
| Cross 波束对基础超载移出 `k` 内循环 | 在上一项上再快 2.16% | 10/10 一致 | 保留 |

容量分母优化只把原来每次执行的整数下限校验和整数到 double 转换移到
初始化；除法及其操作数数值不变。Cross 优化保持原表达式的运算顺序，
只是复用同一 `(i,b2)` 波束对在整个任务桶扫描期间不变的前半部分。

两项合并后的最终同轮成对 A/B：

| seed | `d238f9a` CPU 秒 | 最终版 CPU 秒 | 节省 |
|---:|---:|---:|---:|
| 1 | 8.27705 | 7.93843 | 4.09% |
| 2 | 9.08471 | 8.69743 | 4.26% |
| 3 | 8.40718 | 8.05956 | 4.13% |
| 4 | 9.61317 | 9.19671 | 4.33% |
| 5 | 9.30815 | 8.87870 | 4.61% |
| 6 | 8.58843 | 8.21083 | 4.40% |
| 7 | 8.59229 | 8.26131 | 3.85% |
| 8 | 8.22549 | 7.88319 | 4.16% |
| 9 | 9.11261 | 8.71681 | 4.34% |
| 10 | 8.78638 | 8.39614 | 4.44% |

合计由 87.99546 秒降至 84.23911 秒，CPU 时间减少 **4.27%**，
吞吐提升 **1.0446x**。10/10 的完整 `profit@phase`、最终解和全部
非计时 PROFILE 计数完全一致，且全部通过 `Solution verified OK`。

整数放宽阈值另以 `denom=1..100000`、规则网格和随机 `rho` 对边界两侧
做了穷举校验；原浮点判定与整数阈值判定一致。最终源码也通过
`-Wall -Wextra -Wpedantic`、AddressSanitizer 和 UndefinedBehaviorSanitizer
短时运行检查。
